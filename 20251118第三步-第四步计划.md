# 第三步（计划设置）与第四步（任务完成要求）规划草案

## 目标概述
移动端发布流程需支持一次性及重复性任务，并明确完成要求（时间窗口/附件/位置等）。为避免一次迭代过大，分为“必须项”（当前迭代）与“后续扩展”两组。

---

## 第三步：任务计划设置（Plan）

### Must-have 范围
1. **一次性任务**  
   - 仅设置目标日期（可选）＋开始/结束时间窗口（可选）。  
   - 结束日期默认与开始同日；若提供时间窗口，则用于核对提交时间。

2. **固定重复任务（每天/每周）**  
   - 支持“每天”“每周”两种模式。  
   - 每周模式可选具体星期；默认周一至周五。  
   - 每个周期提供“必须完成窗口”（日粒度＋可选时间段）。
   - 结束条件：  
     * 永不结束（默认）；  
     * 自定义结束日期。  

3. **任务生成策略**  
   - 本次迭代：沿用“每个周期生成一条任务记录”逻辑（一次性写入 n 条，或后台按周期触发）。  
   - 若上一周期未完成，也继续生成下一周期（先保持简单）；在 UI 上加提醒“若需要暂停，请在任务列表中终止”。  

4. **计划 UI 草案**  
   - Step 3 提供三个卡片：一次性 / 每天 / 每周（单选）。  
   - 具体参数（截止日、开始-结束时间、重复周期、结束条件）放在卡片内展开表单。  
   - 所有控件先以 placeholder 实现（例如日期选择器调用现有 `DatePickerModal`），不涉及新的数据库结构。

### Nice-to-have / 后续
1. **自定义重复**（每月/自定义间隔/指定日期序列）——未来引入 `cron` 或规则引擎。  
2. **结束条件扩展**：重复次数、最大实例数、与节假日挂钩。  
3. **任务生成规则**：未完成的任务是否允许延期、是否自动跳过下一周期、手动触发下一周期。  
4. **定时发布**：可设定未来某时间自动创建一次性任务（类似定时器）。  
5. **暂停/恢复**：对正在运行的重复任务进行批量暂停。  

若在 UI 需要展示这些后续功能，使用 disabled 或 `Coming soon` 标签以免阻断流程。

---

## 第四步：任务完成要求（Completion Requirements）

### Must-have 范围
1. **附件与留言要求**  
   - 开关：需附件 / 需文本备注。  
   - 仅允许使用手机拍照/录像（可选开关；启用后 AttachmentPicker 只暴露 Camera/Video 选项）。
   - 附件大小、数量限制：延用现有 20MB 上限，后续可加数量阈值。

2. **时间限制**  
   - “允许随时完成” vs “仅在有效窗口内完成”。  
   - 时间窗口与 Step 3 的开始/结束保持一致，若 Step 3 没提供则默认全天。

3. **“自动验收/自动归档”**  
   - 已在上一迭代加入开关，本步需补充说明：开启自动验收时，当附件/留言达标即可自动完成；是否自动归档使用现有布尔值。

### Nice-to-have / 后续
1. **GPS/位置打卡**：上传位置信息 + 定位偏差容忍范围（需要新表结构）。  
2. **多种附件要求**（拍照 + 问卷 + 文本组合）——可引入表单模板。  
3. **录像时长限制**、多附件类型白名单。  
4. **逾期处理**：过期后是否允许补交、补交时是否通知管理员、是否自动生成下一任务。  
5. **验收流程**：手动验收与退回理由记录，需与“我发布的任务”联动。  

---

## 数据/结构影响
1. **计划字段**  
   - 现有 tasks 表：预留 `schedule_type`（one-time / daily / weekly）、`schedule_config`（JSON，存储周期细节）、`deadline`。  
   - 重复任务若采用单条记录 + schedule_config，需要后台调度；若一次性插入多条任务需考虑 UI 提示。  
   - 后续扩展字段：`time_window_start`, `time_window_end`, `max_occurrences`, `auto_pause`.

2. **完成要求字段**  
   - 当前 `tasks` 已有 `require_attachment`；需新增 `require_text`, `camera_only`, `time_window_enforced`。  
   - 未来位置/打卡：`require_location`, `location_radius`, `location_lat`, `location_lng`.  

---

## 建议的实现顺序
1. **Step 3 - UI 搭建**  
   - 卡片 + 单选 + 日期/时间控制；一次性与每天/每周联动。  
   - 数据结构先存在 `scheduleDraft`（本地对象），提交时转为 API payload。  

2. **Step 4 - 现有字段完善**  
   - 完成要求卡片：附件开关、文本开关、时间窗口开关。  
   - 复用 Step 3 的窗口设置（如果 Step 3 没提供，则禁用时间窗口开关）。

3. **占位/禁用说明**  
   - 自定义重复、自定义结束条件、GPS 打卡等，用 disabled 条目 + “Coming soon” 描述。  
   - 保持 payload 中的字段结构，以便后续扩展。

---

## 验收标准
1. 用户可在 Step 3 选择三种任务类型（一次性 + 每天 + 每周），并配置最基本的截止日期/时间信息。  
2. Step 3 选择结果保存为结构化草稿（`scheduleDraft`）。  
3. Step 4 可设置附件/备注要求、时间窗口 enforcement，并与 Step 3 结果联动。  
4. 所有未实现功能需有明显的 disabled 状态 + 提示文本，避免用户误以为不可用是 bug。  

---

## 后续里程碑（第 II 期之后）
1. 自定义重复/高级结束条件/定时发布。  
2. GPS/位置/签到类要求、录像时长限制。  
3. 自动生成下一周期的状态控制 + 逾期处理策略。  
4. 细化验收与退回流程，包含自动提醒与结果统计。  
