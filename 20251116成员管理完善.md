# 20251116 成员管理完善方案

## 目标

- 在组织管理入口新增“成员管理”页，按角色顺序（Owner → Admin → Member）展示所有成员。
- 行信息：用户昵称（可被管理员双击编辑）、锁定标记（红=锁定、绿=可编辑）、标签完成状态（红=未完成、绿=已完成）。Owner/Admin 在任何状态下可编辑标签。
- 锁定昵称后，成员端的昵称编辑入口禁用；解锁后恢复。
- 视觉延续组织/标签的浅色卡片 + 红绿状态色体系，保持移动端统一体验。
- 本轮补充：支持角色调整（Member ↔ Admin；Owner 仅转移所有权场景另行约束）和成员删除（双重确认）。

## 数据/权限梳理

- 数据表：`organization_members`（role, display_name, status）、`member_tags`、`organization_tag_categories`。
- 需要扩展：
  - `organization_members` 增加 `display_name_locked boolean default false`（需迁移 + RLS）。
  - RLS：Owner/Admin 可更新成员 display_name 与锁定标记；成员只能改自己且未锁定时允许。
- 标签完成判定：沿用 `member_tags` 与必填分类对比逻辑（reuse MembershipSection 的批量查询方案）。

## 体验设计

- 入口：在组织卡片内的“管理成员”按钮，进入独立页面/模态。
- 列表排序：先按 role（owner/admin/member），同角色按加入时间 asc。
- 行布局：
  - 左：昵称（若锁定=显示锁图标红；可编辑=绿），下行可加邮箱/用户 ID 的浅灰小字。
  - 中：标签状态芯片（红/绿，与组织中心一致），可点击打开标签抽屉（Owner/Admin 永远可点；成员若被锁定昵称仍可点标签）。
  - 右：统一“设置”按钮（齿轮/三点）打开行内操作面板，面板内包含：
    - 编辑昵称（禁用态灰色当锁定）
    - 锁定/解锁昵称
    - 调整角色（Member ↔ Admin；Owner 的变更需后续单独流程，当前仅允许 Owner 操作且不能将自己降级为无 Owner 状态）
    - 移除成员（双重确认：确认框 + 二次输入/按钮）
  - 视觉精简：避免行内过多图标拥挤，把操作集中到设置面板。
  - 双击昵称触发编辑；长按/按更多进入操作菜单。
- 批量刷新：进入页面时批量拉取所有成员的必填标签完成度，避免单行触发多请求。
- 反馈：保存后 Toast + 行内状态立刻变色；失败弹出原因。

## 技术方案

- 组件拆分
  - `MembersManagerScreen`：页面容器（列表、刷新、权限判断）。
  - `MemberRow`：渲染单行（昵称/锁定/标签状态/操作）。
  - `useMemberTagStatus(orgId)`：批量获取成员必填完成状态（参考 `MembershipSection` 新增的批量查询逻辑，按 member_id 返回 missing 数）。
  - `EditMemberNameSheet`：底部抽屉，含锁定切换。
  - 复用 `useTagManagement` + 选择抽屉，传入目标 member。
- API 调用
  - 昵称修改：`update organization_members set display_name`（权限依据 RLS）。
  - 锁定切换：`update organization_members set display_name_locked = true/false`。
  - 标签编辑：沿用现有 `member_tags` 写入逻辑，并在保存后刷新 tag 状态。
  - 状态批量查询：
    - 必填分类：`organization_tag_categories (is_required=true)` by org.
    - 成员选择：`member_tags -> organization_tags(category_id)` by member_ids.
- 状态管理
  - 本地 map：`tagStatusByMemberId = { missing: number }`，初始加载即填充；保存后重新计算。
  - 锁定态同步 memberships hook：保存成功后调用 `useOrganizationMembers` 刷新或局部更新行状态。

## 迭代步骤

1. 数据 & RLS：新增 `display_name_locked` 字段 + 政策；生成迁移。
2. Hook：实现 `useMemberTagStatus(orgId)` 批量查询。
3. UI：创建 `MembersManagerScreen` + 行组件 + 设置面板（包含昵称/锁定/角色调整/移除）+ 标签编辑入口。
4. 入口：在组织卡片（默认组/其他组）“管理成员”按钮跳转/打开页面。
5. 测试回归：Owner/Admin/Member 的编辑/锁定/标签保存；角色调整的权限 & 禁用路径；移除成员的双重确认；标签状态首屏正确。

## 待确认

- 角色调整/移除成员是否在本迭代内开放？（预留菜单占位）。
- 默认组移除成员即移出组织，是否需要二次确认弹窗文案。
- 颜色值沿用现有红/绿样式：`membershipActionButtonWarning` / `Ready`，锁定图标可重用同色系。
