# 我的任务清单（发布者视角）规划稿

## 1. 入口与导航
- **入口位置**：沿用组织中心→“我的标签”卡片区域，在其下方新增 `我的任务` 入口按钮。保持与既有模块一致的卡片式外观，默认以淡紫色背景呈现，hover/press 态加深。
- **标签状态同步**：入口角标可沿用红/绿书签逻辑显示“待处理数量”，并在任务全完成时置为绿色。
- **导航方式**：点击后打开全屏抽屉（覆盖当前组织中心），顶部保留返回箭头＋标题“我发布的任务”，方便回到上一层。

## 2. 任务列表布局
- **整体色块**：延续 Step1/2/3 的浅灰卡片背景（#f8fafc）+圆角20组合。列表顶部保留统计信息条（淡紫底）显示：进行中/待审批/已归档数量。
- **卡片信息层级**：
  - 标题：加粗20px，右上角显示任务状态标签（进行中=蓝，待审批=橙，已归档=灰）。
  - 次级信息：
    - 截止日期 / 重复类型（图标+文本）。
    - 自动化开关摘要（auto accept / auto archive / 时间窗），使用小型 pill，启用时绿色，禁用时灰色。
  - 进度条：显示“已完成 / 总执行人”，颜色跟状态保持一致。
  - 操作条：
    1. `审核`（主要按钮）——打开审批面板。
    2. `详情`（次要按钮）——进入任务明细抽屉。
    3. 更多菜单（···）——包含“暂停重复”“删除任务”“复制链接”等。

## 3. 任务详情抽屉
- **触发方式**：点击“详情”或卡片本体。抽屉从底部滑出，固定高度 80%，内含滚动区域。
- **内容结构**：
  1. 基本信息：标题、描述、发布组织、创建时间。
  2. 计划信息：展示 Step3 设置的类型、日期/时间窗口、重复范围（颜色沿用 Step3 的 schedule pill）。
  3. 完成要求：以色块列表显示 attachment / note / camera-only / time-window，启用为绿色背景+对号，禁用灰色。
  4. 执行人列表：分组展示（Owner 始终在顶部），每行显示头像、昵称、状态、提交时间。可在此直接点入审批。
- **审批入口**：详情页内提供“审批所有待处理”按钮，点击后打开专门的审批抽屉。

## 4. 审批面板（重点交互）
- **表现形式**：独立抽屉（覆盖 90% 屏幕），顶部显示任务标题+当前筛选状态。
- **筛选 Tabs**：`待审批` / `已通过` / `已退回` / `全部`。默认落在待审批。
- **成员行信息**：
  - 左侧显示昵称+角色 badge（owner/admin/member），中部显示提交摘要（时间、附件数量、备注前几字），右侧显示状态图标。
  - 行背景：待审批=浅橙，已通过=浅绿，退回=浅红，未提交=灰。
- **操作按钮**：
  - `查看提交`：打开成员提交详情（附件+备注+标签)。
  - `通过` / `退回`：按钮配色绿/红；退回需弹出文本框填写原因。
  - `删除执行人任务`：放在更多菜单中，二次确认。
- **批量操作**：顶部提供 `全选/清空`、`批量通过`、`批量退回`。批量退回时统一填写原因，自动发送到所有选中的成员。
- **自动化提示**：若任务启用了 auto-accept，则面板顶部提示“成员提交后自动转为完成；仍可手动退回”。

## 5. 列表交互/色块规范
- **状态色**：
  - 进行中：#2563eb 蓝色
  - 待审批：#f97316 橙色
  - 已归档：#94a3b8 灰色
  - 成员状态：完成=绿(#22c55e)，退回=红(#ef4444)，未提交=灰(#cbd5f5)
- **Hover/Press**：卡片 hover 提升阴影；按下时背景轻微变深。
- **Skeleton/空态**：使用淡蓝色骨架条；空态显示插图+“还没有发布任务，先去发布一个吧”按钮。

## 6. 下一步接口与测试要点
1. **API/查询**：复用 Web 端 `tasks` 查询，按 `created_by = session.user.id`，需要分页（默认 10 条，下拉加载）。
2. **审批操作**：调用 `task_assignments` 更新/退回 API，在失败时允许离线队列。
3. **自动归档**：保持现有布尔字段，后续在后台任务中处理；列表仍提供手动归档按钮。
4. **测试策略**：
   - 验证启用/禁用 auto-accept + time-window 时，UI标识是否正确。
   - 审批批量操作：网络失败时提示并回滚选中状态。
   - 夜间模式预留：色块需采用 theme token，未来适配深色模式。

> 以上 plan.md 将作为“我的任务”页面的实现参考，下一步可先搭建抽屉/列表骨架，再接入 Supabase 查询与审批 API。

## 7. Web 端可借鉴元素
- **详情/附件体验**：`apps/web/app/dashboard/tasks/components/TaskDetailPanel.tsx` 中的附件分区、分页器与下载/删除操作可直接移植。移动端可精简为：附件列表 + “下载/删除”图标 + 轻量分页控件，以保持桌面端的审核逻辑一致。
- **审批逻辑**：`apps/web/app/dashboard/tasks/hooks/use-task-dashboard/detail.ts` 已实现 `onReview`（接受/退回）和 `task_assignments` 状态更新，可复用其 Supabase 调用结构与错误处理。
- **列表过滤**：Web 端 `TaskList.tsx` 使用查询参数 + 状态 Filter（进行中/待审批/已完成）。移动端可借鉴其 `useTaskDashboard` store，将发布者任务按 `archived_at`、`status_summary` 分类，以保证和 Web 仪表盘看到的数据一致。
- **Composer 逻辑**：`TaskComposer.tsx` / `TaskComposerAssigneeSelector.tsx` 的 tag 过滤、批量追加与模板体验与我们 Step1/Step2 已保持一致；未来可共用 `use-task-dashboard/tag-filters.ts` 中的序列化方法，减少双端差异。

## 8. 数据库对齐
- Web 端目前未提供“手动归档”按钮，但底层 `tasks` 在 `0001_core_schema.sql` 中已经包含 `archived_at` 字段，移动端无需新增列即可支持归档/撤销归档。
- 批复退回依赖 `task_assignments.review_status` & `review_note`（同样已在 `0001` 定义），移动端审批面板可直接写入这些列并复用 Web 端逻辑。
