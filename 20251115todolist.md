# 2025-11-15 To-Do（移动端账户/组织体系）

## 0. 模型共识

- **默认群组=全体成员容器**：所有新成员先进入默认群组；从此群组移除=移出组织；其他群组只是子集合，移除仅影响该群组。
- **群组创建入口**：仅组织管理员可在“系统设置”中创建，来源成员必须从组织当前成员挑选，禁止外部直接入组。
- **任务可见性**：任务对整个组织可见，成员只看到任务所属组织，不暴露具体群组，群组更多用于权限与批量操作。

## 1. 组织系统设置面板（齿轮入口）

1.1 **结构与样式**

- 新建 `OrgSettingsSheet`（复用 `Modal + orgCreateOverlay` 浅色风格），组件内按卡片切分：概览、默认群组、其他群组、计划提示。
- 所有卡片使用独立背景色（淡蓝/淡绿/淡灰）+圆角，标题排版与 AccountSection 保持一致。

  1.2 **组织概览卡**

- 展示：组织名、slug、可见性、计划等级+到期、成员/群组占用情况（取自 plan limits + `members.length`、`groups.length` placeholder）。
- 追加动作按钮占位：`Leave organization`（成员态）、`Delete organization`（owner 态）——禁用但有文案。

  1.3 **默认群组卡**

- 固定文案说明“默认群组 = 全体成员”；显示当前成员数、不可删除/重命名标记。
- 按钮：`管理成员`（跳转现有成员列表）、`查看移除规则`（弹出提示说明移除=退出组织）。

  1.4 **其他群组列表**

- 数据源：`useOrganizationGroups`（占位 hook，可先用 mock），每行包含名称、成员数、一个“Default”标签隐藏。
- 操作按钮：`创建群组`（点击开抽屉表单，限制 Free 用户最多 5 个）、`编辑`、`删除`、`成员管理`（除非无 API，则展示 disabled + 提示）。

  1.5 **Coming soon / 安全控件**

- 占位卡列出未来功能：可见性切换、通知偏好、自动审批等，维持 UI 稳定。

## 2. 组件与状态拆分

- 新增组件：`OrgSettingsSheet.tsx`、`OrgOverviewCard.tsx`、`OrgGroupList.tsx`、`OrgGroupForm.tsx`。
- hooks：`useOrganizationGroups`（暂返回默认群组 + 空数组）、`usePlanLimits` 已有，用于显示 quota。
- 将齿轮按钮 `onPress` -> `setOrgSettingsVisible(true)`；在 `AccountScreen` 底部渲染新的 Modal。

## 3. 交互与验证

- **表单校验**：群组名称 2-12 字符、描述可选、默认群组不可编辑。
- **反馈**：所有创建/更新/删除调用 Supabase RPC 后 toast/alert 通知，并刷新本地列表。
- **空态**：当仅有默认群组时，显示提示“尚未创建其他群组”，同时展示创建按钮。

## 4. 视觉一致性

- 所有浅色卡片背景取自现有 palette（浅蓝 `#e0f2fe`、浅绿 `#dcfce7`、浅灰 `#f3f4f6`）。
- 按钮风格沿用 `primaryButton` / `secondaryButton`；图标统一 Ionicons（settings-outline, people, add-circle）。
- 确保弹出的抽屉层级高于 Org Hub/AccountSection（放在 `AccountScreen` 最底部渲染）。

## 5. 后续准备

- 整理 Supabase 层：默认群组 RLS、成员同步、群组表结构（待确认 migrations）。
- i18n：补充 `account.organization.settings.*`, `account.groups.*` 中/英文。
- 测试流程：`pnpm --filter mobile lint` + 手动走创建/编辑/删除路径，验证免费限额提示。

## 6. 未来阶段（标签与成员入口）

- 标签管理（移动端）：
  - 复用网页端标签列表/筛选逻辑，在移动端任务页提供“标签快速过滤”抽屉；需要先审查 web 端 Tag 管理组件、API（`organization_tags`, `group_tags` 等）。
  - 计划在任务页顶部“发现/过滤”区域提供入口，展示常用标签 pill + “全部标签”按钮，便于后续扩展。
- 组织加入/审批入口：
  - 对照 web 端加入组织、审批加入流程（邀请码、join requests、审批 API），在移动端账户页的“成员/加入管理”折叠块内实现。
  - 提供两个子页：`加入新组织`（输入邀请码/二维码）与 `审核申请`（列表 + 批准/拒绝动作），并在首页或账户 Tab 提醒管理员。
