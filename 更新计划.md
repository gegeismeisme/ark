# 移动端任务清单更新计划

> 作为“控制器”，该文档记录每一次移动端任务清单的新增、完成、阻塞及决定。更新节奏需同步开发进度。

## 🎯 目标

- 📱 在移动端完整承接“任务下发—执行—反馈”的闭环，桌面与移动互通。
- 🌐 全链路中英文双语（基于 `apps/mobile/src/i18n/en.json` 与 `zh.json`）。
- ⚙️ 离线优先：任务草稿、附件、状态更新均可在弱网下保存并自动补偿。
- 📊 以老师/组织视角输出洞察与提醒，保证大规模使用时的可观测性。

## 📆 阶段拆解

### 1. 导航与通用体验

- [x] 统一底部导航为图标化 Tasks🗂️ / Publish📝 / Insights📈 ，并完成 EN/ZH 文案（2025-11-09）。
- [x] 引入 `useActiveOrganization`，支持本地记忆有效组织（2025-11-09）。
- [ ] 为 `OrgProvider` 增加错误重试、空态 CTA（跳转 `/organizations` 申请公开组织）。
- [ ] 在 `OrgSwitcher` 中展示成员数 / 角色标签，降低大组织切换成本。

### 2. 任务模型与详情

- [x] 任务列表支持图标过滤 chips（all/today/week/overdue），并本地缓存离线数据（2025-11-09）。
- [x] 抽象共享任务模型（任务元信息 + Checklist + 附件 + 执行人），输出到 `packages/shared` 供 Web & Mobile 复用（2025-11-10）。
- [x] 丰富任务详情视图：Checklist 可编辑、附件时间线、review state（待批/通过/驳回）徽标（2025-11-10）。
- [ ] 新增 App 级状态 Toast/Badge，同步离线/重试反馈。

### 3. 发布模板与分享

- [x] 移动端 `PublishForm` 支持活跃组织快捷指定模板字段（2025-11-09）。
- [x] 构建模板库：支持多场景（课程/班级/巡检）预设字段、动态 Checklist（2025-11-10）。
- [x] 生成分享 / 深链目标（短信、钉钉、校内 IM），复用 Web 发布逻辑（2025-11-10）。

### 4. 离线与上传增强

- [x] 附件上传使用 `offlineAttachments` 持久化 AsyncStorage 缓存（2025-11-09）。
- [x] 建立 Pending 队列：Checklist 结果 + 附件 + 状态变更统一排队、自动重试（2025-11-10）。
- [x] UI 层提供离线提示、失败重试、逐项还原（含 badge & banner）（2025-11-10）。
- [ ] 对齐 Web 端 Dexie 结构，准备共享本地缓存协议。

### 5. Insights & Analytics

- [x] 初版 Insights 展示任务数、完成率趋势、Top 模板榜（2025-11-09）。
- [ ] 扩展完成率维度：按模板、按组织、按时间段，加入提醒触达统计。
- [x] 重构统计拉取：allowedGroupIds 与 summary 并发，请求分页/聚合接口以降低首屏等待（2025-11-10）。

### 6. 测试与质量（质量门）

- [ ] 为 `useAssignments`、`useInvites`、`taskStore`、offline 队列等新增 Vitest 覆盖。
- [ ] 在 CI 中验证 i18n 键值（EN/ZH 同步）+ ESLint 检查移动端特有规则。
- [ ] 规划 Detox / Maestro 关键路径（登陆 → 选择组织 → 创建任务 → 待办完成）。

## 📝 动态记录

- **2025-11-09**：完成阶段 1 导航改版、任务列表与 PublishForm/Insights 初版、附件缓存。下一阶段聚焦共享模型与详情体验。
- **2025-11-10**：重写本计划为 Unicode 版本，纳入 Org/任务/模板/离线/质量等六大板块，并确认剩余待办顺序。
- **2025-11-10**：完成共享任务模型抽象 + Web/Mobile 一致化映射，并上线 Checklist 编辑、附件时间线、审核状态视觉反馈。
- **2025-11-10**：发布模板扩展 4 大场景（课堂/巡检/会议/校园运行），自动补全 Checklist、截止时间及深链分享，移动端表单实时预览模板细节。
- **2025-11-10**：上线离线 Pending 队列与同步提示（Banner+Badge），任务状态可离线提交并自动重试。
- **2025-11-10**：移动端本地缓存对齐 Web Dexie 键位规范（localCache），附件缓存复用统一 Snapshot 结构。
- **2025-11-10**：Web Analytics 并行化 allowed-group + summary 拉取，并仅按需请求任务元信息，减小首屏加载时间。
- **2025-11-10**：移动端附件上传支持离线排队（pendingDrafts），附件面板可提示、重试与删除失败记录。
- **2025-11-10**：Web OrgProvider 自动重试 + OrgSwitcher 成员数标签上线，空态 CTA 引导前往 /organizations。
