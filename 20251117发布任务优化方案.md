# 20251117 发布任务优化方案（移动端 vs Web 对比与优化计划）

## 现状对比
- Web：任务发布流程已跑通，表单结构清晰，配色与块状布局较统一；下�?选择组件样式成熟�?- Mobile：功能可用但体验欠佳——抽屉半屏遮挡、内容溢出截断；色块/层级与当前“组�?标签”统一风格不符；模板和执行人选择组件耦合在同一文件（`apps/mobile/src/features/publish/PublishForm.tsx` 200+ 行），复用性差�?- 实时性：成员/标签等信息刷新已补偿，但发布表单未复用这些刷新机制，成员列表偶尔旧数据�?
## 主要痛点
- 视觉：缺少我们常用的浅色块分�?卡片化；弹窗按钮与全局主色不统一；滚动区域不固定，长文案易被遮挡�?- 布局：标�?模板/执行�?附件全部堆在一个视图，缺少分组标题和间距；附件区域占位不明显�?- 组件化：表单逻辑�?UI 混杂，无法复用执行人选择、模板卡片、附件列表等子块；难以单独调试�?
## 设计与交互方�?- 容器：保持底部弹出（BottomSheet/Modal），但改为“定�?+ 头部留白 + ScrollView”结构，按分区滚动，不截断内容�?- 色块/分区：沿用组�?标签的浅色卡片风格�?  - 模板区：淡灰背景 + 贴纸风模�?pill（图�?标签色区分）�?  - 执行人区：浅蓝卡片；选中态深色填充、未选中灰白�?  - 必填提示：红色小�?边框；提交失败弹 Alert�?- 组件化（新建文件，便于维护）�?  - `PublishTemplateList.tsx`：模板列�?选择�?  - `AssigneeSelector.tsx`：成员选择，可复用 `useOrganizationMembers`+分页/搜索�?  - `AttachmentPicker.tsx`：展示已选附件、添�?删除入口，尺寸提示�?  - `PublishHeader.tsx`：顶部标题、副标题（组织名）与关闭按钮�?  - `PublishFooter.tsx`：固定底部提交按钮、loading/禁用态�?  - `PublishForm.tsx` 保留�?orchestrator（状�?提交），瘦身�?- 可用性：
  - 滚动区与底部按钮分离，保证按钮始终可见�?  - 必填字段校验：标题、组织、至少一执行人�?  - 错误提示：在表单顶部展示错误条（红色浅底），�?Alert 兜底�?
## 数据与逻辑
- 成员数据：复�?`useOrganizationMembers`，在表单初始与回到前台时自动 refresh（可�?`useAppStateRefresh`），避免旧成员列表�?- 模板：沿用现�?`PUBLISH_TEMPLATES`，但 UI 改用可滚动的水平标签卡�?- 提交�?  - 先创�?task，再插入 assignees（task_members），附件上传递延（先写占位，上传�?patch `attachments` 字段或附件表）�?  - 失败时回退已创建的 task（可选：标记草稿状态）�?- 国际化：新增分组标题、错误条、附件提示等 i18n key�?
## 视觉规格（建议）
- 背景：整体淡�?灰背景，卡片白底/浅灰�?- 选中态：主色深蓝文本 + 深色描边或填充；未选中浅灰�?- 必填/错误：边�?背景红；选填区域灰；成功/正常无强调�?- 按钮：主操作深色实底，次级淡色描边�?
## 拆分与落地步�?1) 创建组件文件（上�?6 个），将现有 `PublishForm` 中对�?UI、交互拆分迁移�?2) 重构布局：头�?+ ScrollView（模�?标题/执行�?描述/附件/截止日期/开关）+ 固定底部按钮�?3) 增加错误条与必填校验提示；提交时 loading/禁用�?4) 复用 `useAppStateRefresh`/refreshMembers，确保执行人列表实时�?5) 样式落地：在 `layoutStyles.ts` 补充发布模块专用样式（卡片、pill、底部栏）�?6) 补充 i18n 文案（标题、分组、错误提示）�?
## 风险与验证
- 风险：拆分后样式引用遗漏、状态提升漏传；执行人刷新依赖 orgId 变化时的订阅。
- 验证：模拟器/真机下多成员/多模板/附件上传；必填校验；断网失败提示；提交成功自动 reset/关闭弹窗。
## 增量讨论：四步滑动式发布 & 多次筛选追�?- 四步流程（左右滑�?Pager）：1) 主题/备注/附件�?) 选择组织/�?+ 标签过滤 + 追加成员（显示已选人数，可全�?取消）；3) 任务类型（一次�?重复，提供简化的常用重复规则）；4) 达标要求（附件开关、截止日�?时间段、仅拍照开关）�?- 标签筛选追加：每次按条件过滤出成员，勾选后“追加到已选”；已选列表独立存储（Set 去重），支持多次筛选叠加；UI 显示“本次匹�?X 人，已选累�?Y 人”，提供清空/全�?批量取消�?- 滚动与按钮：每页定高 ScrollView，底部按钮固定；卡片化分区，必填项用红色提示�?
## 已发任务的编�?终止（“我发布的任务”入口）
- 允许：标题、描述、附件要求、截止时间、执行人增删（未开�?进行中时）；终止任务（标�?`cancelled`/`archived`，阻止后续提交）�?- 权限：创建者或组织管理员；其他人只读�?- UI：在“我发布的任务”列表（放在“我的组�?我的标签”附近）增加编辑/终止入口；编辑复用发布表单带初始值；终止二次确认，提示受影响人数�?- 后端：使用状态字段（`status` �?`archived_at`）；任务成员变更时通知；复杂重复规则大改建议“复制为新任务、旧任务终止”�?
## 下一步计划（细化）
1) 组件拆分落地：新增 PublishHeader/TemplateList/AssigneeSelector/AttachmentPicker/PublishFooter，瘦身 PublishForm。
2) 四步 Pager 骨架：实现分屏导航、进度条、底部按钮固定。
3) 成员筛选追加：AssigneeSelector 支持标签过滤、多次追加、已选人数显示，全选/清空。
4) 样式与配色：发布模块专用卡片/按钮样式，统一浅色块与主色交互。
5) 编辑/终止入口：在“我发布的任务”列表加入操作，复用发布表单/加入终止确认。
6) 校验与提示：必填校验、错误条、提交/终止 loading 状态；i18n 补全。

## 验收与归档（自动/手动）
- 自动验收：发布时提供“自动验收”开关（默认关闭）。开启后满足提交条件（附件/时间窗口）即自动置为 completed，可选“自动归档”移出活跃列表。
- 手动退回：在“我发布的任务”列表或任务详情里提供“退回/重新开启”，将状态改回进行中，执行人需重新提交。
- 批量操作：在“我发布的任务”列表支持多选后批量归档/退回/删除（删除仅限草稿或未开始），二次确认。
- 配置入口：发布表单第 4 步“达标要求”中增加“自动验收”“自动归档”开关；列表侧可覆写（权限控制）。
- 审计：自动/手动验收、退回、归档都记录事件（task_events）便于追踪。

