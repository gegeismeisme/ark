# 2025-11-15 我的标签方案（移动端）

## 1. 入口与信息架构

- 将 Account 页面中原「Organization management」卡片改为「My tags」。
  - 卡片标题与描述强调“标签是组织协作的必备条件”。
  - 绿色按钮替换为“Open tag center”，点击后进入 Tag Management Center（下称 TMC）。
- TMC 是标签相关的唯一工作界面，聚合管理员视角与成员视角。
  - 顶部采用与 Org Hub 相同的半透明背景。
  - 分成两块：**Admin tools**（仅 owner/admin 可见）与 **My assignments**（所有成员可见）。

## 2. 数据来源与 Hook 规划

- 复用 Web 端逻辑：`organization_tag_categories`, `organization_tags`, `member_tags`, `tag_requests`.
- 新 Hook 列表：
  1. `useTagCategories(orgId)` – 查询类别 + tags + scope/required/selection.
  2. `useMemberTagAssignments(orgId, userId)` – 拉取用户的 `member_tags` + pending requests。
  3. `useTagRequests(orgId)` – 管理员审批标签申请（web 使用 `resolve_tag_request`）。
- 需要新增 `member_tags` 的自编辑 policy，允许成员操作自身条目（与 display_name 策略类似）。

## 3. 管理员功能

- **类别列表**：每个类别卡片显示名称、Scope（Org/global or Group list）、Required badge、Selection type。
- **操作**：
  - **Create category** sheet：字段（Name、Scope=Organization/Group、Required toggle、SelectionType single/multiple）。
  - **Edit category** sheet：同上，但允许删改 tags。
  - **Add tag**：底部 chips +文本输入，支持启用/禁用标签。
  - **Delete category/tag**：确认弹窗，引用 Web 的限制提示（若有未完成的 tag 请求需先处理）。
  - **View pending tag requests**：跳转到请求列表模块，管理员可 approve/reject。
- UI：沿用 pastel 卡片，每个 sheet 高度与 JoinOrganization Drawer 对齐。

## 4. 成员功能

- **My assignments**：按 Required Category → Optional Category 排列。
  - Required 未完成时显示 warning badge。
  - 进入类别详情（Bottom sheet）后，根据 selection type 决定单选 / 多选 UI，提交后写入 `member_tags` 或生成 `tag_requests`（若类别需要审批）。
  - 显示历史申请（approved/rejected）并允许删除记录（同 join 请求逻辑）。
- 若 owner/admin 也需要填标签，同样使用此模块。

## 5. 权限与错误处理

- 所有 write 操作遵循 web 的 RPC：
  - `create_tag_category`, `update_tag_category`, `delete_tag_category`, `submit_tag_request`, `resolve_tag_request`, etc.（参考 web 的 `use-tag-management.ts`）。
- 成员提交标签失败时（RLS 拒绝/重复提交）要在 UI 中提示——复用 Join 页面错误提示逻辑。
- 加入 Realtime 监听（类似 `useUserMemberships`）以便标签审批/填写后自动刷新。

## 6. 视觉细节

- “My tags” 卡片使用浅紫色（#ede9fe）背景，白色按钮 + 紫色 icon，保持与 My organization 一致。
- TMC 的卡片背景：管理员工具用淡紫，成员 assignments 用淡蓝，强调两个区块。
- 图标提议：
  - 类别卡：`pricetag`、`layers`、`shield-check`（表示 required）。
  - 成员卡：`information-circle`（详情）、`alert-circle`（缺项）、`checkmark-circle`（完成）。

## 7. 迭代步骤

1. 新入口 + 空的 TMC Shell（仅显示“Coming soon”）。
2. 实现 `useTagCategories` + Admin 列表/新增/编辑。
3. 实现 `useMemberTagAssignments` + My assignments UI。
4. 加入 Tag Request 管理（管理员审批、成员历史）。
5. Realtime & polish：动画、空态、错误提示。
