# 2025‑11‑15 我的标签方案（移动端）

## 1. 参考 Web 的核心逻辑
- Web 端 `apps/web/app/dashboard/tags/use-tag-management.ts` 将 **类别、标签、成员、请求** 统一在一个 hook 中处理，暴露 Create/Update/Delete/Resolve 等 RPC；我们在移动端也保持单一入口，减少状态分裂。
- Web UI 拆成两个区块：**管理员管理**（category CRUD + tag request 审批）和 **成员自助填写**；移动端 Tag Center 继续沿用这一结构。
- 功能等价：底层仍调用 `organization_tag_categories / organization_tags / member_tags / tag_requests` 以及 `create_tag_category`, `update_tag_category`, `delete_tag_category`, `submit_tag_request`, `resolve_tag_request` 等 RPC，保证移动端与 Web 一致。

## 2. 数据与 Hook 规划
1. `useTagManagementMobile(orgId, userId)`：封装 Web hook 的核心逻辑，返回：
   - `categories`: 带 required/scope/selection/tags 的结构。
   - `memberAssignments`: 当前成员在每个类别的选择、缺失情况、pending 请求。
   - `tagRequests`: 管理员视角的待审批列表。
   - mutations: `createCategory`, `updateCategory`, `deleteCategory`, `toggleTag`, `submitAssignment`, `requestTags`, `resolveRequest`, `refresh`.
2. 对 `member_tags` 新增 RLS（若尚未补齐）以允许成员更新自己的 display_name/标签。
3. 所有 hook 默认监听 Supabase Realtime（categories、member_tags、tag_requests 三个 channel），保持界面自动刷新。

## 3. 手机端界面规划
### 3.1 入口（已完成）
- `My tags` 浅紫卡片 + review 图标 + `>`，保持与「我的组织」一致。

### 3.2 Tag Management Center 布局
- 顶部 Stay consistent with Org Hub：back 按钮 + 标题 +描述。
- 主体用 ScrollView，分为两个卡片：
  1. **Admin tools**（仅 owner/admin 可见）：
     - 列表：每个类别卡展示名称、required 徽章、作用域（org 或 group）、selection type、已启用标签数量。
     - 行为：`+`（创建类别）、`⋯`（编辑/删除/添加标签）、`通知`图标（进入请求审批）。
     - 采用折叠 + 内嵌 FlatList，长列表时支持搜索/筛选（按 group/required/selection）。
  2. **My assignments**（所有成员可见）：
     - 两部分：Required & Optional。缺失时显示 warning badge。
     - 点击类别 → Bottom sheet（如 org create drawer），支持 single-select radio、multi-select chips。
     - 如果类别需要审批，则提交 request 并在 sheet 中提示“待审核”。
     - “History” 按钮跳入历史记录列表（最多 10 条，可删除）。

### 3.3 大量标签时的 UX
- 顶部 Filter bar：搜索框 + chips（Required、Optional、Group filter、Selection type）。
- 列表内部使用虚拟滚动（FlatList windowing）避免一次性渲染上百个标签。
- 提供索引导航：例如 group/category 名称在顶部生成 anchor，可快速跳转。
- 批量操作模式（管理员视图）：长按类别进入“多选”状态，以批量启用/禁用标签。

### 3.4 交互与状态
- 所有 bottom sheet、弹窗统一复用 org drawer 样式；保存/审批后显示 toast（成功/失败）。
- 失败文案复用 join-request 逻辑：`Alert.alert` + i18n Key。
- Pending request 的数量显示在首页 My tags 图标上，提示管理员处理。

## 4. 迭代里程碑
1. **Shell 完成**（✅）：Tag Center UI 卡片 + placeholder。
2. **Hook 接入**：实现 `useTagManagementMobile` 并拉取 categories/memberAssignments/tagRequests。
3. **成员填写**：Required/Optional 列表 + bottom sheet 选择 + submit/request 流程。
4. **管理员管理**：类别 CRUD + 标签启停 + 审批 drawer。
5. **搜索/筛选/虚拟列表**：提升大数据量体验。
6. **Realtime & polish**：状态同步、loading、空态、动画、错误兜底。

## 5. 设计准则
- Pastel 色块、圆角、阴影与现有 Account 页面保持一致。
- 文案与 Web 共用翻译 key（新增 mobile 专用 key 时保持命名一致，如 `dashboard.tags.*`）。
- 清晰的 badge/pricetag/shield icon，帮助用户区分角色与操作。

> 以上计划已同步 Web 端逻辑，后续开发按里程碑推进，并在测试阶段根据体验反馈调整。
